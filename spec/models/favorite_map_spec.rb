require 'spec_helper'

describe FavoriteMap do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    FavoriteMap.create!(@valid_attributes)
  end
end
